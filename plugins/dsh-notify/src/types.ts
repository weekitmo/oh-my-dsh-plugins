export type { AttentionEntry, AttentionTone, DingTalkNotification, DingTalkPublicSettings, DingTalkSettingsUpdate, NotificationReason, NotificationSettings, NotifyProjectionValue, TitleAnimation } from './contract.ts'

export interface ResolvedConfig {
  readonly maxBodyChars: number
}
